package de.tomfreund.customermap;

import org.junit.jupiter.api.Test;

class CustomerMapApplicationTests {

    @Test
    void contextLoadsPlaceholder() {
        // Voller Context-Load-Test folgt mit Testcontainers (Roadmap).
        // Bewusst kein @SpringBootTest ohne Datenbank im CI.
    }
}
